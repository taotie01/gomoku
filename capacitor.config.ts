import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.example.gomoku",
  appName: "五子棋",
  webDir: "public", // 根据实际情况修改为 dist、public 或其他目录
};

export default config;
